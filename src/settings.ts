interface Settings {
  title: string;
  fixedHeader: boolean;
  showLogo: boolean;
}

const settings: Settings = {
  title: "PI-CLOUD",
  fixedHeader: true,
  showLogo: true,
};

export default settings;
